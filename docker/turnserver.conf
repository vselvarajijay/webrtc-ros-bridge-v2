# Minimal coturn config for WebRTC TURN (Docker).
# Credentials: user=scout, password=scout (set TURN_USERNAME/TURN_CREDENTIAL in .env to match).
listening-ip=0.0.0.0
listening-port=3478
realm=scout.local
lt-cred-mech
user=scout:scout
