# MCP server for ConnectX robot
FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir mcp[cli] httpx

COPY mcp/server.py ./server.py

ENV MCP_PORT=8002
ENV PYTHONUNBUFFERED=1

EXPOSE 8002

CMD ["python", "server.py"]
