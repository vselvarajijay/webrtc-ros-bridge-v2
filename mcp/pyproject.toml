[project]
name = "connectx-mcp"
version = "0.1.0"
description = "MCP server for ConnectX robot: state, perception images, and control"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "httpx",
]
